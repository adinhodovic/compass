{
  "name": "portal-assets",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i assets/input.css -o internal/server/static/app.css --minify",
    "build:js": "node tools/build-assets.mjs",
    "build": "npm run build:css && npm run build:js",
    "test:e2e": "npm run build && playwright test"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "@tailwindcss/typography": "^0.5.0",
    "alpinejs": "^3.14.0",
    "daisyui": "^5.0.0",
    "fuse.js": "^7.0.0",
    "htmx.org": "^2.0.0",
    "iconify-icon": "^3.0.0",
    "mermaid": "^11.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "esbuild": "^0.28.0"
  }
}
